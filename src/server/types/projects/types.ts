export interface ProjectData {
  name: string;
  website: string;
  twitter: string;
  monthlyUsers: string;
  avatar: string;
  shortDescription: string;
  description: string;
  id: string;
  maker: string;
}

export type ProjectsData = ProjectData[];
